# 韡宁的 UI 品味档（她挑出来的「好看」）

> 这是一个**会成长的档案**。每次「生成一批 UI → 韡宁挑她喜欢的」，就把她的选择和理由记在这里。
> 生成新 UI 前**先读这个档**，让产出偏向她已经表达过的品味；挑完后**追加新纪录**。
> 目标：让「好看」从抽象变成她本人的具体偏好，越用越准。

## 怎么维护这个档（每轮流程）

1. 生成一批 UI（用 `style-library.md` 选不同风格方向，标注每个用了哪个风格 #）。
2. 韡宁挑出她喜欢的（和明确不喜欢的）。
3. 在下面「纪录」区追加一行：日期、她选了哪个风格/手法、她的原话/理由、我的归纳。
4. 累积几轮后，把反复出现的偏好升级到「已确认偏好」区（当规则用）。

## 已确认偏好（当规则，生成时默认套用）

> 目前累积中——等有几轮挑选后填入。先放一个从既有互动观察到的起点：
- （待填）倾向：______
- （待填）避免：______

## 纪录（每轮追加，最新在上）

<!-- 格式：
### YYYY-MM-DD 第N批
- ❤️ 喜欢：#38 Neubrutalism v2 的硬阴影按钮 —— 她说「这个撞色但不刺眼」→ 归纳：喜欢有个性但配色协调
- ❌ 不喜欢：#41 Cyberpunk —— 她说「太吵」→ 归纳：避免高饱和霓虹叠加
-->

（还没有纪录——第一批生成后开始累积。）

### 2026-08-17 · 系统备注
- 本次執行時，`b-battle-580b5-default-rtdb.firebaseio.com` 被本 session 的網路政策擋下（proxy 403），無法讀取 ❤️ 資料，故本輪未能追加品味紀錄。第一批 6 個 UI 已照常生成並發布，之後若能連上 Firebase 需補讀歷史 ❤️ 資料。
